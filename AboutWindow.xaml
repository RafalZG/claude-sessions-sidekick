<Window x:Class="ClaudeSessionsSidekick.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About"
        Width="420" SizeToContent="Height"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        KeyDown="Window_KeyDown">

    <Window.Resources>
        <Style TargetType="ToolTip">
            <Setter Property="Background" Value="#2A2A3A"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderBrush" Value="#40606080"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#3A3A5A"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderBrush" Value="#40606080"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="Transparent">
                            <Border x:Name="Bd" Background="#3A3A5A"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    Padding="{TemplateBinding Padding}" CornerRadius="2">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#4A4A6A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="8" Background="#1E1E2E" BorderBrush="#40606080" BorderThickness="1"
            Margin="8">
        <Border.Effect>
            <DropShadowEffect BlurRadius="12" ShadowDepth="3" Opacity="0.5" Color="Black"/>
        </Border.Effect>

    <Grid Margin="24,20,24,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- App name and version -->
        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,16">
            <TextBlock Text="Claude Sessions Sidekick" FontSize="20" FontWeight="Bold"
                       Foreground="#A0B0FF" HorizontalAlignment="Center"/>
            <TextBlock x:Name="txtVersion" Text="Version 1.0.0" FontSize="12"
                       Foreground="#888899" HorizontalAlignment="Center" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Description -->
        <TextBlock Grid.Row="1" TextWrapping="Wrap" FontSize="12" Foreground="#C0C0D0"
                   TextAlignment="Center" Margin="0,0,0,16"
                   Text="A Windows tray companion for Claude Code that monitors usage limits, tracks token consumption, manages sessions, and helps you work smarter with context-aware recommendations."/>

        <!-- Separator -->
        <Border Grid.Row="2" Height="1" Background="#30606080" Margin="0,0,0,16"/>

        <!-- Links -->
        <StackPanel Grid.Row="3" Margin="0,0,0,16">
            <TextBlock Text="Links" FontSize="13" FontWeight="SemiBold" Foreground="#A0B0FF"
                       Margin="0,0,0,8"/>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                <TextBlock Text="&#x2B50;" FontSize="12" Margin="0,0,8,0" VerticalAlignment="Center"/>
                <TextBlock x:Name="lnkGitHub" Text="GitHub Repository" FontSize="12"
                           Foreground="#6EA8FF" Cursor="Hand" TextDecorations="Underline"
                           MouseLeftButtonDown="LnkGitHub_Click"
                           ToolTip="View source code, report issues, and contribute"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                <TextBlock Text="&#x1F41B;" FontSize="12" Margin="0,0,8,0" VerticalAlignment="Center"
                           FontFamily="Segoe UI Emoji"/>
                <TextBlock x:Name="lnkIssues" Text="Report a Bug / Request a Feature" FontSize="12"
                           Foreground="#6EA8FF" Cursor="Hand" TextDecorations="Underline"
                           MouseLeftButtonDown="LnkIssues_Click"
                           ToolTip="Open a GitHub issue to report bugs or suggest new features"/>
            </StackPanel>

        </StackPanel>

        <!-- Separator -->
        <Border Grid.Row="4" Height="1" Background="#30606080" Margin="0,0,0,16"/>

        <!-- Credits -->
        <StackPanel Grid.Row="5" Margin="0,0,0,16">
            <TextBlock Text="Credits" FontSize="13" FontWeight="SemiBold" Foreground="#A0B0FF"
                       Margin="0,0,0,8"/>
            <TextBlock FontSize="12" Foreground="#C0C0D0" TextWrapping="Wrap">
                <Run Text="Created by"/><Run Text=" Rafał Zygmunt" FontWeight="SemiBold"/>
            </TextBlock>
            <TextBlock FontSize="11" Foreground="#888899" Margin="0,4,0,0"
                       Text="Built with WPF (.NET 10) and Claude Code"/>
            <TextBlock FontSize="11" Foreground="#888899" Margin="0,2,0,0"
                       Text="Licensed under MIT License"/>
        </StackPanel>

        <!-- Update check + Close -->
        <Grid Grid.Row="6">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Left">
                <TextBlock x:Name="lnkCheckUpdate" Text="Check for updates" FontSize="11"
                           Foreground="#6EA8FF" Cursor="Hand" TextDecorations="Underline"
                           MouseLeftButtonDown="LnkCheckUpdate_Click"
                           ToolTip="Check GitHub for a newer version"/>
                <TextBlock x:Name="txtUpdateStatus" FontSize="11" Foreground="#888899"
                           Margin="8,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>
            <Button Content="Close" HorizontalAlignment="Right"
                    Padding="24,6" Click="BtnClose_Click"/>
        </Grid>
    </Grid>
    </Border>
</Window>
